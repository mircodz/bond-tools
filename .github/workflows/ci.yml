name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  version-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Fail if version was not bumped
        run: |
          VERSION=$(cat version)
          git fetch --tags --force
          if git rev-parse "$VERSION" >/dev/null 2>&1; then
            echo "::error file=version::Version $VERSION is already tagged. Bump the version file before merging." >&2
            exit 1
          fi
          echo "Version $VERSION has no existing tag."

  build-and-test:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      pull-requests: write

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: Restore dependencies
      run: dotnet restore

    - name: Build
      run: dotnet build --no-restore --configuration Release

    - name: Test with coverage
      run: |
        dotnet test --no-build --configuration Release \
          /p:CollectCoverage=true \
          /p:CoverletOutputFormat=cobertura \
          /p:ExcludeByFile="**/*.g4.cs"

    - name: Generate coverage report
      uses: danielpalme/ReportGenerator-GitHub-Action@5.3.11
      with:
        reports: 'out/test-results/**/coverage.cobertura.xml'
        targetdir: 'out/coverage-report'
        reporttypes: 'HtmlInline;Cobertura;MarkdownSummaryGithub;TextSummary'
        tag: '${{ github.run_number }}_${{ github.run_id }}'

    - name: Read coverage summary
      id: coverage
      if: always()
      run: |
        if [ -f out/coverage-report/Summary.txt ]; then
          echo "summary<<EOF" >> $GITHUB_OUTPUT
          cat out/coverage-report/Summary.txt >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

          # Extract line coverage percentage
          LINE_COVERAGE=$(grep "Line coverage:" out/coverage-report/Summary.txt | grep -oP '\d+\.?\d*(?=%)')
          echo "line_coverage=$LINE_COVERAGE" >> $GITHUB_OUTPUT

          # Extract branch coverage percentage
          BRANCH_COVERAGE=$(grep "Branch coverage:" out/coverage-report/Summary.txt | grep -oP '\d+\.?\d*(?=%)')
          echo "branch_coverage=$BRANCH_COVERAGE" >> $GITHUB_OUTPUT
        else
          echo "summary=Coverage report not found" >> $GITHUB_OUTPUT
          echo "line_coverage=0" >> $GITHUB_OUTPUT
          echo "branch_coverage=0" >> $GITHUB_OUTPUT
        fi

    - name: Add coverage summary to GitHub step summary
      if: always()
      run: |
        echo "## Code Coverage Summary" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        if [ -f out/coverage-report/SummaryGithub.md ]; then
          cat out/coverage-report/SummaryGithub.md >> $GITHUB_STEP_SUMMARY
        else
          echo "Coverage report not generated" >> $GITHUB_STEP_SUMMARY
        fi

    - name: Post coverage comment on PR
      if: github.event_name == 'pull_request'
      uses: marocchino/sticky-pull-request-comment@v2
      with:
        header: coverage
        message: |
          ## Code Coverage Report

          **Line Coverage:** ${{ steps.coverage.outputs.line_coverage }}%
          **Branch Coverage:** ${{ steps.coverage.outputs.branch_coverage }}%

          <details>
          <summary>View detailed coverage</summary>

          ```
          ${{ steps.coverage.outputs.summary }}
          ```

          </details>

          ---
          *Coverage report generated for commit ${{ github.sha }}*

    - name: Upload coverage report artifact
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: coverage-report
        path: out/coverage-report/
        retention-days: 30
